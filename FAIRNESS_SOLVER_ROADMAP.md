# خارطة طريق: حلّال العدل (Fairness Solver)

> **ذاكرتنا المشتركة.** نرجع إليها كلّ جلسة لنعرف أين وصلنا، وما التالي، ولئلّا ننسى أو نتوه.
> آخر تحديث: 2026-06-19.

> 🫀 **القلب الحقيقيّ = النظر-للأمام (`solveLookahead`).** يعالج المقاعد **الأكثر تقييدًا
> أوّلًا** ويوزّع بمفتاح (الحِمل ثمّ الحداثة ثمّ الطاقم)، فيُحجَز المُجبَر على مقعده المستقبليّ
> ويُرتاح في يومٍ **قبل** الحدث — هذا «الامتصاص قبل الحدث» الذي بُني كلّ شيءٍ لأجله.
> الحلّالات الأماميّة (`solveDisturbance`/`rebalanceDays`) أصبحت **حالةً خاصّةً/سلّمًا** —
> المسار الحيّ سيستعمل `solveLookahead`. درسٌ مستفاد: نبني أخطر/أمركز جزءٍ أوّلًا.

---

## الهدف

قلبٌ جديدٌ للعدل **يفصل الذكاء عن الحساب**: مقياسٌ صريحٌ للعدل + حلّالٌ يصلح الخلل **اللحظيّ**
بأقلّ تدخّل، عبر **الامتصاص قبل الحدث** (استعمال كلّ الأيّام التي لم تقع بعد، حتى ما قبل الحدث).
يحلّ تدريجاً محلّ التسوية السببيّة-الأماميّة للعجلة، **دون أن نخسر** أيّ ضمانٍ حاليّ.

## المبادئ الحاكمة (لا نكسرها)

1. **الظلّ أوّلاً:** كلّ قدرةٍ جديدة تُبنى **قراءةً فقط** (تحسب، تقترح، تُدقّق، لا تكتب) وتُختبر، **قبل** أيّ كتابةٍ حيّة.
2. **لا كتابة حيّة حتى الاكتمال:** لا نربط الحلّال بالكتابة الحيّة حتى **يغطّي كلّ الحالات في الظلّ ويُختبر**. (تصحيحٌ لخطأٍ سابق: «الكتابة الحيّة» ليست الخطوة التالية مباشرةً.)
3. **قابلية العودة دائماً:** الوسم `stable-causal-wheel` نقطة عودةٍ للقلب القديم. كلّ مرحلةٍ كوميتٌ مستقلّ.
4. **لا نكشف الآليّة للمستخدم** (سرّ تجاريّ): لا «خوارزميات/حلّال» في كلام الذكاء للأطبّاء.
5. **اللمس بقدر الحاجة:** الحلّال لا يحرّك إلا ما تأثّر فعلاً — صفر بلا حدث، وبقدر الظلم مع الحدث.

---

## النموذج الذهنيّ: محاور vs قيود

ليس كلّ ما ذُكر «يُعاد توزيعه». نفصل:

- **محاور العدل** (يوازنها الحلّال بالحداثة): المنفرد · الدليقيتر · **الاحتياطي (EX)** · **تناوب عيادة البورد**.
- **القيود** (يحترمها الحلّال ولا «يوازنها»): **التخفيف** (يجب أن يُزاوَج، لا ينفرد) · **تغطية الاستئذان** (يجب أن يُغطّى) · **النقص** (لا يمكن ملء عيادةٍ بلا طبيب). هذه تُشكّل **الأهليّة/الإتاحة** المُدخَلة للحلّال.

وبُعدٌ عموديّ على الكلّ: **عبر الأيّام** (الحلّال قد يلمس شفتات عدّة أيّام لتحقيق العدل).

---

## الحالة الآن (✅ منجز / ⬜ متبقٍّ)

### المرحلة أ — إكمال الحلّال في الظلّ (قراءةٌ فقط)

| # | القدرة | الحالة | الملفّ/الاختبار | كوميت |
|---|---|---|---|---|
| ① | **المقياس + المراقب** (حداثة، كشف نقاط) | ✅ | `lib/algorithms/fairness.ts` · `test-fairness` (3/3) | `7f6f466` |
| ② | **الحلّال — ميزان الفترات** (ف١−ف٢) | ✅ | `lib/algorithms/solver.ts` · `test-solver` (10/10) | `c534d69` |
| ②ب | **الحلّال — الأدوار الثقيلة بالحداثة** (انفراد/دليقيتر) | ✅ | `solver.ts` · `test-solver-heavy` (8/8) | `c6b01a7` |
| ③ | **إيصال التدقيق** (قبل/بعد، حفظ النقاط) | ✅ | مدمجٌ في الحلّالين | — |
| ④ظ | **جسر التنسيق بالظلّ** (عجلة vs حلّال، فترات) | ✅ | `test-coordination` (31/31) | `fcdf613` |
| ④ظ٢ | **استخراج المقاعد من جدولٍ حقيقيّ** + صقل الأهليّة | ✅ | `solver.ts` · `test-coordination-heavy` (6/6) | `35c732c`,`db69053` |
| ④ح | **الحلّال المُوجَّه بالحدث (شفت واحد، لمسٌ بقدر الحاجة)** | ✅ | `solveDisturbance` · `test-disturbance` (9/9) | `4b1e98f` |
| **A1** | **التوازن عبر الأيّام** (لمس شفتات أكثر من يوم) | ✅ | `rebalanceDays` · `test-rebalance-days` (10/10) | `88459da` |
| **A1+** | **تمحيصٌ صارم: ١/٢/٣ أسابيع + غيابُ يومٍ محدّد (absentByStamp) + لا تموّج رجعيّ** | ✅ | `test-rebalance-rigorous` (13/13) | `5d5a314` |
| **🫀 القلب** | **النظر-للأمام / الامتصاص قبل الحدث** (`solveLookahead`) | ✅ | `test-lookahead` (10/10) · `test-lookahead-real` (11/11، ٣ أسابيع) | `cbc8b6f`,(هذا) |
| **A2** | **محور الاحتياطي (EX)** — راحةٌ بالحداثة، **الغياب يُحسب راحة** | ✅ | `extractReserveSeats`/`lastRestStamps` · `test-reserve` (5/5) | (هذا) |
| **A3** | **محور تناوب عيادة البورد** | ⬜ | — | — |

> **حدود A1 المُثبَتة (صدقًا):** مُختبَرٌ على **الدليقيتر** عبر ١/٢/٣ أسابيع (أساسٌ صفر لمس،
> غيابُ يومٍ محدّد، امتصاصٌ مبكر، تموّجٌ أماميٌّ عبر الأسابيع، لا لمسَ رجعيّ، حدودٌ مضبوطة).
> **لم يُختبَر الانفراد** (٢١ طبيبًا/٣ عيادات لا تُنتجه — يلزم تقليص البِركة). البورد/التخفيف/
> النقص خارج A1 (A3–A6).
>
> **حقيقةٌ مصحّحة:** الاحتياط **صغير** هنا (~٢/شفت، لا ١٤ كما قُدِّر — الجدول صباحٌ ومساء فالعمل
> أكثر). محوره **مختلف**: راحةٌ بالحداثة (`solveHeavyRecency` لا `solveLookahead`)، و«الغياب راحة».
> **حدود A2:** الاحتياط مُتشابكٌ مع توزيع العيادات (تغيير المحتاط ⇄ تغيير العامل) — عُولج كمحورٍ
> مستقلٍّ بالحداثة؛ التوزيع الكامل للعيادات (العبء الأكبر) لم يُبنَ بعد.
| **A3** | **محور تناوب عيادة البورد** | ⬜ | — | — |
| **A4** | **قيد التخفيف** (يُزاوَج، لا ينفرد) في الأهليّة | ⬜ | — | — |
| **A5** | **قيد تغطية الاستئذان** (مَن يغطّي) ربطاً بمسار الطلبات | ⬜ | — | — |
| **A6** | **وعي النقص** (عيادات فارغة) — يحترمه لا يخلق أطبّاء | ⬜ | — | — |

### المرحلة ب — بوّابة التحقّق (ما زال بلا كتابة)
- ⬜ **B1**: تشغيل الحلّال جنباً إلى جنبٍ مع القلب/الطلبات على **كلّ** الحالات أعلاه، ومقارنة الإيصالات — برهان «إمّا يوافق أو يحسّن، ولا يُسيء أبداً».

### المرحلة ج — الكتابة الحيّة (خلف علم، عيادة الاختبار فقط)
- ⬜ **C1**: ربط مسار الطلبات (غياب/عودة/استئذان) بـ `solveDisturbance`/`rebalanceDays` وتطبيق الإيصال، مع إبقاء القلب الحاليّ بديلاً.
- ⬜ **C2**: مقارنة النتائج الحيّة، ثمّ توسيعٌ تدريجيّ، ثمّ إزالة العلم.

---

## الترتيب الموصى به (تصاعديّ، بلا قفز)

1. ~~**A1 — عبر الأيّام**~~ ✅ (`rebalanceDays`: غيابٌ في يومٍ يتموّج لأيّامٍ لاحقة، غيابٌ في يومين يلمس يومين؛ صفر لمس بلا حدث).
2. **A2 → A3 — الاحتياطي ثمّ البورد** (محورا عدلٍ إضافيّان، نفس نمط الإيصال). ← **التالي**
3. **A4 → A6 — القيود** (تخفيف، استئذان، نقص) كأهليّة/إتاحة.
4. **B1 — التحقّق الشامل** جنباً إلى جنب.
5. **C1 → C2 — الكتابة الحيّة** خلف علمٍ ثمّ التوسيع.

> القاعدة: لا ننتقل لرقمٍ أعلى قبل أن يكون الأدنى **مبنيّاً ومُختبَراً وكوميت**.

---

## جرد الاختبارات (كلّها قراءةٌ فقط)

`test-fairness` · `test-solver` · `test-solver-heavy` · `test-coordination` · `test-coordination-heavy` · `test-disturbance`
تشغيل: `npx tsx --env-file=.env scripts/<name>.ts` (الثابتة بلا `--env-file`).
عيادة الاختبار: `10000000-0000-0000-0000-000000000001` · أسبوع `2099-01-04`.

## نقاط العودة
- وسم `stable-causal-wheel` = القلب القديم المستقرّ.
- كلّ صفٍّ في الجدول أعلاه = كوميتٌ مستقلّ قابلٌ للعودة إليه.

## ملفّاتٌ مؤقّتة تُحذف قبل الإصدار
`scripts/exp-*` · `scripts/diag-*` · `scripts/seed-sick-leave.ts` (تجريبيّة).
